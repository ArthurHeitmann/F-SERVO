import 'package:flutter/material.dart';

class OpenFileArea extends StatelessWidget {
  const OpenFileArea({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("OpenFileArea");
  }
}

